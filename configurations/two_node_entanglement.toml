# Two-node entanglement generation (Barrett-Kok)
# Equivalent experiment to examples/two_node_barrett_kok.rs

[topology]
type = "linear"
nodes = 2
distance_km = 10.0
attenuation_db_per_km = 0.2
memory_slots = 8
source = 0
dest = 1

[hardware]
profile = "erbium"

[simulation]
target_pairs = 100
max_time_ms = 500000.0
